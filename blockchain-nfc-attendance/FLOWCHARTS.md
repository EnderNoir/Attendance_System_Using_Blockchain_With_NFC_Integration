# 🎯 System Flowcharts & User Guides

## 1. Student Registration Flow

```
╔════════════════════════════════════════════════════════════════╗
║              STUDENT REGISTRATION PROCESS                      ║
╚════════════════════════════════════════════════════════════════╝

┌─────────────┐
│ Home Page   │
└──────┬──────┘
       │ Click "Register Student"
       ▼
┌──────────────────────┐
│ Registration Form    │
│ - Wallet Address     │
│ - Student ID         │
│ - Name               │
│ - NFC ID             │
└──────────┬───────────┘
       │ Submit
       ▼
┌──────────────────────┐
│  Validate Input      │
│  - Check address     │
│  - Verify NFC ID     │
└──────────┬───────────┘
       │
       ├─ Invalid ─────────► Show Error ◄─ Return to Form
       │
       └─ Valid
           │
           ▼
    ┌─────────────────────┐
    │ Build Transaction   │
    │ - Call contract     │
    │ - Add gas estimate  │
    └──────────┬──────────┘
           │
           ▼
    ┌─────────────────────┐
    │ Send to Blockchain  │
    └──────────┬──────────┘
           │
           ▼
    ┌─────────────────────┐
    │ Wait for Confirm    │
    └──────────┬──────────┘
           │
           ├─ Failed ─────────► Show Error ◄─ Retry Option
           │
           └─ Success
               │
               ▼
        ┌─────────────────────┐
        │ Show Success Message│
        │ - Tx Hash           │
        │ - Student Registered│
        └─────────────────────┘
```

---

## 2. Attendance Marking Flow

```
╔════════════════════════════════════════════════════════════════╗
║           ATTENDANCE MARKING PROCESS                           ║
╚════════════════════════════════════════════════════════════════╝

┌──────────────┐
│ Dashboard    │
└──────┬───────┘
       │
       ├──► Subject Input
       │    [Mathematics  ]  ◄────┐
       │                          │
       ├──► NFC Input             │
       │    [Tap NFC Card ]        │
       │         │                 │
       │         ├─ No Card ──────►│ (Waiting for input)
       │         │                 │
       │         └─ Card Detected  │
       │              │            │
       │              ▼            │
       │         ┌──────────┐      │
       │         │ Read NFC │      │
       │         │ ID: NFC001  ◄──┘
       │         └────┬─────┘
       │              │
       ▼              ▼
    ┌──────────────────────┐
    │ Click "Mark Present" │
    └──────────┬───────────┘
           │
           ▼
    ┌──────────────────────┐
    │ Validate             │
    │ - NFC ID exists?     │
    │ - Student active?    │
    │ - Subject entered?   │
    └──────────┬───────────┘
           │
           ├─ Invalid ───────► Show Error
           │
           └─ Valid
               │
               ▼
        ┌─────────────────────┐
        │ Build Transaction   │
        │ - markAttendance()  │
        └──────────┬──────────┘
               │
               ▼
        ┌─────────────────────┐
        │ Send to Blockchain  │
        └──────────┬──────────┘
               │
               ▼
        ┌─────────────────────┐
        │ Wait for Confirm    │
        └──────────┬──────────┘
               │
               ├─ Failed ──────► Retry
               │
               └─ Success
                   │
                   ▼
            ┌──────────────────┐
            │ Success Alert    │
            │ Refresh Records  │
            │ Clear Inputs     │
            └──────────────────┘
```

---

## 3. Data Retrieval Flow

```
╔════════════════════════════════════════════════════════════════╗
║            VIEW ATTENDANCE RECORDS FLOW                         ║
╚════════════════════════════════════════════════════════════════╝

┌──────────────┐
│ Dashboard    │
│ Load Page    │
└──────┬───────┘
       │
       ▼
┌──────────────────────┐
│ Check System Health  │
│ /api/health          │
└──────────┬───────────┘
       │
       ├─ Disconnected ────► Show Warning
       │
       └─ Connected
           │
           ▼
    ┌──────────────────────┐
    │ Fetch All Records    │
    │ /api/all-records     │
    └──────────┬───────────┘
           │
           ▼
    ┌──────────────────────┐
    │ Process Records      │
    │ - Filter by date     │
    │ - Count statistics   │
    │ - Sort by newest     │
    └──────────┬───────────┘
           │
           ▼
    ┌──────────────────────┐
    │ Display             │
    │ Statistics:          │
    │ - Total Records      │
    │ - Today's Attendance │
    │ - Active Students    │
    │                      │
    │ Table:               │
    │ [All Records Listed] │
    └──────────┬───────────┘
           │
           ├─ User Clicks "Export CSV" ──► Generate CSV ──► Download
           │
           ├─ User Searches Student ──────► Query & Display
           │
           └─ Auto-refresh (60s) ─────────► Repeat from "Fetch"
```

---

## 4. System Architecture Diagram

```
╔═══════════════════════════════════════════════════════════════════╗
║                    SYSTEM COMPONENTS                              ║
╚═══════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────┐
│                      WEB BROWSER                                │
│  ┌────────────────────────────────────────────────────────┐    │
│  │              Frontend Layer                             │    │
│  │  • index.html (Landing page)                           │    │
│  │  • dashboard.html (Dashboard)                          │    │
│  │  • style.css (Responsive design)                       │    │
│  │  • main.js (Navigation logic)                          │    │
│  │  • dashboard.js (Dashboard logic)                      │    │
│  └────────────────┬─────────────────────────────────────┘    │
│                   │                                            │
│                   │ HTTP Requests (JSON)                       │
│                   │                                            │
└───────────────────┼────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                    FLASK SERVER                                 │
│              http://127.0.0.1:5000                             │
│  ┌────────────────────────────────────────────────────────┐    │
│  │              API Layer                                 │    │
│  │  GET/POST: /api/register-student                       │    │
│  │  GET/POST: /api/mark-attendance                        │    │
│  │  GET: /api/attendance-count/<id>                       │    │
│  │  GET: /api/student-info/<addr>                         │    │
│  │  GET: /api/all-records                                 │    │
│  │  GET: /api/health                                      │    │
│  └────────────────┬─────────────────────────────────────┘    │
│                   │                                            │
│  ┌────────────────▼─────────────────────────────────────┐    │
│  │          Business Logic Layer                         │    │
│  │  • Attendance System Class                            │    │
│  │  • Transaction Building                              │    │
│  │  • Data Processing                                   │    │
│  │  • Error Handling                                    │    │
│  └────────────────┬─────────────────────────────────────┘    │
│                   │                                            │
│  ┌────────────────▼─────────────────────────────────────┐    │
│  │          NFC Reader Module                            │    │
│  │  • Hardware Interface                                │    │
│  │  • Simulation Mode                                   │    │
│  │  • Read History                                      │    │
│  └────────────────┬─────────────────────────────────────┘    │
│                   │                                            │
│  ┌────────────────▼─────────────────────────────────────┐    │
│  │          Web3 Interface                               │    │
│  │  • Contract Instance                                 │    │
│  │  • Transaction Creation                              │    │
│  │  • Contract Calls                                    │    │
│  │  • Gas Estimation                                    │    │
│  └────────────────┬─────────────────────────────────────┘    │
│                   │                                            │
│                   │ HTTP (RPC)                                 │
│                   │                                            │
└───────────────────┼────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                 ETHEREUM NODE                                   │
│            (Ganache Local Network)                             │
│         http://127.0.0.1:8545                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │           Blockchain Network                           │    │
│  │  • 10 Accounts (100 ETH each)                          │    │
│  │  • Smart Contract Instance                            │    │
│  │  • Transaction Pool                                   │    │
│  │  • Block Chain Storage                                │    │
│  └────────────────────────────────────────────────────────┘    │
│                   │                                            │
└───────────────────┼────────────────────────────────────────────┘
                    │
                    ▼
           ┌──────────────────┐
           │  NFC Hardware    │
           │  (Optional)      │
           └──────────────────┘
```

---

## 5. Data Model

```
╔═══════════════════════════════════════════════════════════════════╗
║                    SMART CONTRACT DATA                            ║
╚═══════════════════════════════════════════════════════════════════╝

👤 Student Object:
┌──────────────────────────────┐
│ students[address] = {        │
│   studentId: string,         │
│   name: string,              │
│   nfcId: string,             │
│   isActive: boolean          │
│ }                            │
└──────────────────────────────┘

📍 NFC Mapping:
┌──────────────────────────────┐
│ nfcToAddress[nfcId] =        │
│   studentAddress             │
└──────────────────────────────┘

📋 Attendance Record Object:
┌──────────────────────────────┐
│ AttendanceRecord = {         │
│   studentId: string,         │
│   timestamp: uint256,        │
│   subject: string,           │
│   isPresent: boolean         │
│ }                            │
└──────────────────────────────┘

📊 Records Array:
┌──────────────────────────────┐
│ attendanceRecords[] =        │
│ [Record, Record, Record, ...] │
└──────────────────────────────┘
```

---

## 6. Admin Workflow

```
╔════════════════════════════════════════════════════════════════╗
║                  ADMINISTRATOR WORKFLOW                        ║
╚════════════════════════════════════════════════════════════════╝

1. Register Students (Batch)
   └─► Use API: POST /api/register-student
       (Repeat for each student)

2. Set Up Subjects
   └─► In Dashboard, specify subject names
       (e.g., Mathematics, Physics, Chemistry)

3. Monitor Attendance
   └─► Dashboard shows real-time statistics
       └─► Total records
       └─► Today's attendance
       └─► Active students

4. View Specific Records
   └─► Use Search function by Student ID
       └─► See attendance count

5. Export Reports
   └─► Click "Export CSV"
       └─► Use for analysis in Excel/Sheets

6. System Management
   └─► Use CLI tool: python cli.py
       └─► Check status
       └─► View records
       └─► Get attendance counts

7. Troubleshooting
   └─► Monitor /api/health endpoint
       └─► Check blockchain connection
       └─► Verify contract deployment
```

---

## 7. User Interaction Timeline

```
┌─────────────────────────────────────────────────────────────────┐
│                   TYPICAL USER WORKFLOW                         │
└─────────────────────────────────────────────────────────────────┘

Time    Event
────    ─────────────────────────────────────────────────────────

00:00   User opens browser → http://localhost:5000

00:05   Admin: Register 50 students (1-2 min per batch)

00:10   Admin: Prepare attendance form with subject

08:30   Students start: Tap NFC card on reader
        ↓
        Flask backend reads NFC ID
        ↓
        Smart contract marks attendance
        ↓
        Dashboard updates in real-time

08:40   All students checked in

09:30   View attendance report
        ├─ Statistics
        ├─ Individual records
        └─ Export CSV

10:00   Use CLI to verify:
        └─ python cli.py records 20

EOD     Export final report and backup data
```

---

## 8. Error Handling Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    ERROR HANDLING FLOW                          │
└─────────────────────────────────────────────────────────────────┘

Error Type          │ Detection        │ User Message
─────────────────── ┼──────────────────┼──────────────────────
NFC ID Not Found    │ Contract Check   │ "Invalid NFC ID"
Student Not Active  │ Contract Check   │ "Student inactive"
No Address Found    │ Input Validation │ "Invalid address"
Insufficient Gas    │ Transaction Sim  │ "Transaction failed"
Network Disconnect  │ Health Check     │ "Connection lost"
Contract Mismatch   │ Deployment Check │ "Contract not found"
Invalid Input       │ Frontend Validation│ "Please enter valid data"
Blockchain Error    │ RPC Call         │ "Blockchain error"
```

---

## 9. Database Query Examples

```
╔═══════════════════════════════════════════════════════════════════╗
║                COMMON BLOCKCHAIN QUERIES                          ║
╚═══════════════════════════════════════════════════════════════════╝

# Get Student Info
contract.functions.getStudent(address).call()
→ Returns: [studentId, name, nfcId, isActive]

# Get All Records Count
contract.functions.getAttendanceRecordsCount().call()
→ Returns: 125 (total records)

# Get Specific Record
contract.functions.getAttendanceRecord(0).call()
→ Returns: [studentId, timestamp, subject, isPresent]

# Get Attendance Count for Student
contract.functions.getStudentAttendanceCount("STU001").call()
→ Returns: 45 (days present)

# Get NFC Mapping
nfcToAddress["NFC001"]
→ Returns: 0xd41c057fd1cff8d3ebb689728db6d6cb0409945e
```

---

## 10. Transaction Lifecycle

```
┌─────────────────────────────────────────────────────────────────┐
│              BLOCKCHAIN TRANSACTION LIFECYCLE                    │
└─────────────────────────────────────────────────────────────────┘

Step 1: Build Transaction
   ├─ Function call: markAttendance()
   ├─ Arguments: nfcId, subject
   ├─ From address: 0xd41c057...
   ├─ Gas estimate: 80,000
   ├─ Gas price: 2 gwei
   └─ Nonce: auto

Step 2: Sign Transaction
   └─ Signed with private key

Step 3: Submit to Network
   └─ Raw transaction bytes sent to Ganache RPC

Step 4: Process Transaction
   ├─ Validate signature
   ├─ Check sender balance
   ├─ Execute contract function
   ├─ Update blockchain state
   └─ Emit events

Step 5: Block Confirmation
   └─ Transaction included in block

Step 6: Return Receipt
   ├─ Transaction hash
   ├─ Block number
   ├─ Gas used
   ├─ Status (success/failed)
   └─ Logs/Events

Step 7: Frontend Update
   └─ Show confirmation to user
```

---

This comprehensive flowchart guide helps understand:
✅ How students register
✅ How attendance is marked  
✅ How data flows through the system
✅ What the architecture looks like
✅ Common workflows and processes
✅ Error handling
✅ Database operations
✅ Transaction lifecycle
